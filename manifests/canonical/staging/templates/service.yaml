---
# Source: staging/templates/service.yaml
kind: Service
apiVersion: v1
metadata:
  name: canonical-com
  namespace: staging
spec:
  selector:
    app: canonical.com
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
